export default function Loading() {
  return (
    <main className="shell">
      <section className="card">
        <div className="eyebrow">Loading</div>
        <h2>Preparing training environment…</h2>
      </section>
    </main>
  );
}
