# Dependency Security Baseline

The application should track supported Next.js releases and apply security patches promptly. As of the MVP build in August 2026, Next.js 16 is the active major line and official guidance requires Node.js 20.9 or newer.

Before merge or deployment, install the current supported patched release rather than relying on an unbounded major-range assumption.
