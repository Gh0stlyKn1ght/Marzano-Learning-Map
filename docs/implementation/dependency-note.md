# Dependency Strategy

The prototype intentionally keeps the runtime dependency surface small. Before merge, install a supported patched Next.js 16 release and compatible React 19 release, then commit the resolved lockfile used for validation.
