# Teaching Practice Lab App

The executable MVP lives in the repository root alongside the MkDocs knowledge base.

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Validate

```bash
npm run typecheck
npm run build
```

Current branch: `build/mvp-learning-shell`.
